
free c.

(******************** 
  Crypto
********************)

fun HMAC_sha1/2.


(******************** 
  Constants 
********************)

(******************** 
  Concatenation and Parsing 
********************)

data conc1/1.

data conc2/2.

reduc 
  parse3(conc1(x0)) = x0;
  parse3(conc2(x0, x1)) = x0.
reduc 
  parse4(conc2(x0, x1)) = x1.

(******************** 
  <Query>
********************)

query ev:server_recv(x) ==> ev:client_send(x).
query ev:server_recv(x).



(*************************** 
  Model 
***************************)

let A = 
in(c, msg1);
in(c, msg2);
let var1 = parse4(msg2) in
let hash1 = HMAC_sha1(conc1(parse3(msg2)), key) in
if hash1 = var1 then 
event server_recv(parse3(msg2)); 0.

let B = 
event client_send(payload);
let msg1 = conc2(payload, HMAC_sha1(conc1(payload), key)) in
out(c, msg1); 0.

(******************** 
  <Environment>
********************)

process !
  new key;
  in(c, payload);
  (!A | !B)
